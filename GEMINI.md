# 项目专属工作规则 (Workspace Rules)

## 博客部署与发布偏好 (Deployment Preference)
- **直接推送部署**：当在博客中完成文章写作、页面开发、样式调整或配置修改并完成构建验证后，**直接执行 Git 提交并推送到 GitHub 远程仓库 (`git push origin master`)**，由 GitHub Actions 自动化构建部署上线。
- **无需本地预览**：**严禁默认推荐或启动本地开发服务器 (`hexo server`) 进行本地预览**，用户不需要本地预览，一律以线上部署结果为准。
